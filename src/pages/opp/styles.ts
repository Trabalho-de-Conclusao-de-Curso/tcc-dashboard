import styled from 'styled-components';
import { Grid } from '@material-ui/core';

export const Container = styled(Grid)``;

export const OppContainer = styled(Grid)`
    margin-bottom: 10px;
`;

export const RegsContainer = styled(Grid)`
    display: flex;
    flex-direction: column;
`;
